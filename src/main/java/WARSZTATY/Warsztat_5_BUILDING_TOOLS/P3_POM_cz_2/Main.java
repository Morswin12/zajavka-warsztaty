package WARSZTATY.Warsztat_5_BUILDING_TOOLS.P3_POM_cz_2;

public class Main {
    public static void main(String[] args) {
        /*
        mvn clean       - jest lifecycle-m czyści pliki maven-owe, ale można je stworzyć na nowo
        mvn site

        Fazy:
        mvn validate    - służy do np. automatycznie sprawdza struktury naszego kodu (np. ilość znaków w jednej lini, albo żeby zmienna miała min. 2 znaki etc.)
        mvn compile     - kompiluje tworzy katalog target klasy są skompilowane
        mvn test        - służy żeby uruchomić testy automatyczne
        mvn package     - służy do przygotowania paczki (tworzą się pliki.jar)
        mvn verify      - np. pokazuje, jaka część naszego kodu została pokryta testami
        mvn install     - tworzy wszystko i dodaje do lokalnego repozytorium pod podany adres
        mvn deploy      - wsadza naszą paczkę do zdalnego repozytorium

        Włączenie którejś z powyższych komend uruchamia ją oraz wszystkie powyżej (np. compile uruchamia compile, oraz validate)

        cls             - czyści terminal chyba?

         */
    }
}
